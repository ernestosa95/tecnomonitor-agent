# Python basura
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/

# PyInstaller (No subir los .exe, se generan localmente)
build/
dist/
*.spec

# Datos sensibles y Logs (¡CRÍTICO!)
monitor_config.json
monitor_config.json.tmp
activity.log
secret.key
.sql_checkpoint
.sql_checkpoint.tmp


# Inno Setup basura
Output/

# Checksum de rules.json generado por build.bat en cada build (ver
# docs/PLAN_MEJORAS_V4.5.md §3.6) — no se versiona, se regenera siempre
# contra el rules.json que se está empaquetando en ese momento.
rules.json.sha256